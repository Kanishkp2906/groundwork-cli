[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "groundwork-cli"
version = "0.1.0"
description = "Real CVE training for developers — fix production bugs in your terminal"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "Your Name", email = "your@email.com" }
]
dependencies = [
  "httpx",
]

[project.scripts]
groundwork = "groundwork.cli:main"

[project.urls]
Homepage = "https://github.com/yourusername/groundwork-cli"
